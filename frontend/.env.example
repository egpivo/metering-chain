# Demo: enable "Use my Dune key" mode (optional). Key is sent to proxy only, never stored.
# VITE_DEMO_BYOK_ENABLED=true

# Demo proxy base URL. Leave unset in dev when using Vite proxy (npm run demo:server + npm run dev):
# Vite forwards /api to localhost:3001, so same-origin. Set only if you need cross-origin (proxy sends CORS).
# VITE_DEMO_PROXY_URL=
